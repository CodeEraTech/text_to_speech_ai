import React from 'react';
import ReactDOM from 'react-dom/client';
import TextSpeechScreen from './TextSpeechScreen';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TextSpeechScreen />
  </React.StrictMode>
); 